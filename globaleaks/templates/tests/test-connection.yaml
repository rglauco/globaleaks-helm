apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Name }}-globaleaks-test-connection"
  labels:
    {{- include "globaleaks.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ .Release.Name }}-globaleaks:{{ .Values.service.port }}']
  restartPolicy: Never
